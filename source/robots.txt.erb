User-agent: *
Disallow: /cdn-cgi/
<% settings.deployment.locales.each do |locale| %>
Disallow: <%= settings.deployment.base_url_suffix(locale: locale) %>/404.html
<% end %>
<%= settings.development ? "Disallow: /" : "" %>